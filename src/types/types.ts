// Re-export Lang type for convenience
export type Lang = "en" | "es" | "sv";

// Legacy WordPress types - kept for reference but not actively used
// The site now uses Astro content collections instead
