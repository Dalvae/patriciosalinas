---
//index.astro
import HomePage from "../components/HomePage.astro";
import { getLangFromUrl } from "../i18n/utils";

export function getStaticPaths() {
  return [
    { params: { lang: "en" } },
    { params: { lang: "es" } },
    { params: { lang: "sv" } },
  ];
}

const { lang } = Astro.params as { lang: "en" | "es" | "sv" };
---

<HomePage lang={lang} />
